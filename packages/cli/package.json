{
  "name": "@warplane/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI tool for the Interchain Control Plane — operate traces, scenarios, and chains from the terminal",
  "type": "module",
  "bin": {
    "warplane": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@warplane/domain": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "@warplane/storage": "workspace:*"
  }
}
