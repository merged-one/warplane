{
  "name": "@warplane/ingest",
  "version": "0.0.1",
  "private": true,
  "description": "Chain data ingestion pipeline — polls Avalanche nodes and normalises data",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@warplane/domain": "workspace:*"
  }
}
