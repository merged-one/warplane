{
  "name": "@warplane/domain",
  "version": "0.1.0",
  "private": true,
  "description": "Core domain types, schemas, and validation for Avalanche L1 interchain control plane",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo generated",
    "generate": "node scripts/generate-schemas.mjs"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
