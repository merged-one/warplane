{
  "name": "@warplane/domain",
  "version": "0.0.1",
  "private": true,
  "description": "Core domain types and logic for Avalanche L1 interchain control plane",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo"
  }
}
